import { Box } from "@mui/material";
import useStyles from "./useStyles";

const RegisterPage = () => {
  const classes = useStyles();

  return <Box className={classes.pageContainer}>this is register</Box>;
};

export default RegisterPage;
